# Generated by the web `prebuild` step (scripts/copy-overlay.mjs).
# The content-hashed overlay bundle is a build artifact — never commit it.
overlay.*.global.js
overlay.*.global.js.map

# Generated by the web `prebuild` step (scripts/build-fonts-catalog.mjs).
# The font catalog is regenerated on every build — never commit it (U7).
fonts-catalog.json
